# Springfield Progress Log

## Codebase Patterns

> **ALL AGENTS: Read this section FIRST every iteration.**
> Builder and Reviewer both update this section.
> Architect condenses it when it gets too long.
> Format: `Problem: X → Fix: Y` for gotchas, short bullets for everything else.

### Critical Gotchas
> Things that broke and how they were fixed. Read these to avoid repeating mistakes.
- (none yet — Builder and Reviewer add entries here as issues are discovered)

### Tech Stack & Config
> Framework versions, config choices, compatibility notes.
- (populated after US-001)

### Component & Code Patterns
> Reusable patterns, naming conventions, file structure patterns.
- (populated as stories are built)

### Testing Patterns
> Test utilities, mocking patterns, timer handling, assertion tricks.
- (populated as tests are written)

### Design System Values
> Exact colors, fonts, spacing values, component styling conventions from architecture.md.
- (populated after US-002 Visual Theme story)

---

# Phase Log

> Each phase appends entries below. Never delete previous entries.
